# Email delivery for the contact form (https://resend.com)
# Without this key, submissions are logged to the server console instead.
RESEND_API_KEY=

# Where contact requests are sent
CONTACT_TO_EMAIL=hallo@catii.ch

# Verified sender (defaults to Resend's onboarding sender until your domain is verified)
CONTACT_FROM_EMAIL=
